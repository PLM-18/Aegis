# Just some Compiler Stuff

### Compiler Backend Options
1. **LLVM** (Recommended):
   - LLVM provides a complete backend for code optimization and generation
   - Versions 12.0 or newer recommended
   - Requires: `llvm-config`, LLVM development libraries